const express = require('express');
const router = express.Router();

// Index Route
router.get('/', (req, res) => {
    res.render('index', { title: 'E-Book' });
});

// Charge Route
app.post('');

module.exports = router;

